# Runs all ECS service deploy workflows in parallel (build → ECR → CloudFormation).
# SAM/Lambda deploy remains in deploy.yml.
name: deploy-all-ecs-services

on:
  workflow_dispatch: {}
  push:
    branches: [main]
    paths:
      - ".github/workflows/deploy-all-ecs-services.yml"

permissions:
  contents: read

jobs:
  ingestion-pipeline-service:
    uses: ./.github/workflows/deploy-ingestion-pipeline-service.yml
    secrets: inherit

  edital-pipeline-service:
    uses: ./.github/workflows/deploy-edital-pipeline-service.yml
    secrets: inherit

  # Legado (deploy manual): deploy-scraper-runner.yml, deploy-document-processor.yml,
  # deploy-process-edital-service.yml, deploy-validate-edital-service.yml
  # process-edital-service:
  #   uses: ./.github/workflows/deploy-process-edital-service.yml
  #   secrets: inherit
  # validate-edital-service:
  #   uses: ./.github/workflows/deploy-validate-edital-service.yml
  #   secrets: inherit
